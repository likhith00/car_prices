from setuptools import setup, find_packages


setup(
    name = "predict_carprices",
    version = "0.0.1",description="its an mlops implementation of car prices dataset",
    author = "Likhith",
    packages = find_packages(),
    license = "MIT"
)